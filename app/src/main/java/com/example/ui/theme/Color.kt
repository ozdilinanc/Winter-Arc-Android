package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Elegant Dark Void Canvas & Obsidian Surfaces
val CanvasDark = Color(0xFF050608)         // True void black-slate from Elegant Dark
val PanelNavy = Color(0xFF0E1116)          // Rich obsidian card / panel surface
val PanelNavyElevated = Color(0xFF11141B)  // Elevated interactive element container
val PanelNavyHighlight = Color(0xFF1E2433) // Active focus / selected container
val BorderSubtle = Color(0xFF1E293B)       // Refined slate border
val BorderActive = Color(0xFF334155)       // Elevated border / focus ring

// Elegant Dark Accents (Vibrant Indigo & Soft Violet Glow)
val AccentCyan = Color(0xFF6366F1)         // Primary Elegant Indigo
val AccentCyanGlow = Color(0x4D6366F1)     // Indigo glow halo
val AccentIndigo = Color(0xFF818CF8)       // Indigo-400 for tags & links
val AccentPurple = Color(0xFFA855F7)       // Purple-500
val AccentViolet = Color(0xFFC084FC)
val AccentAmber = Color(0xFFF59E0B)        // Amber-500 for XP & Badges
val AccentGold = Color(0xFFFBBF24)         // Amber-400 for Achievement Crowns
val AccentEmerald = Color(0xFF10B981)      // Emerald-500 for completion


// Status Colors (Matching Elegant Dark badges & completion tags)
val StatusNotStarted = Color(0xFF64748B)   // Slate-500
val StatusLearning = Color(0xFFF59E0B)     // Amber
val StatusPracticed = Color(0xFF10B981)    // Emerald-500 (Practiced badge in Elegant Dark)
val StatusCompleted = Color(0xFF10B981)    // Emerald
val StatusStrong = Color(0xFF34D399)       // Mint green

// Branch Accent Palettes (Refined, high-contrast, premium)
val BranchDotNet = Color(0xFF818CF8)       // Sharp Indigo (.NET Core Primary)
val BranchDevOps = Color(0xFFA78BFA)       // Soft Violet (DevOps / K8s)
val BranchAndroid = Color(0xFF10B981)      // Emerald (Android / Mobile)
val BranchCS = Color(0xFFC084FC)          // Lavender (CS Foundations)
val BranchGraduation = Color(0xFFF43F5E)   // Rose / Crimson (Graduation / KV Cache)
val BranchTools = Color(0xFF38BDF8)        // Sky Blue (Git / Linux / LazyVim)
val BranchSecurity = Color(0xFFF59E0B)     // Amber (Security)
val BranchEnglish = Color(0xFF38BDF8)      // Sky Blue (B2+ English)
val BranchPortfolio = Color(0xFF10B981)    // Emerald (Portfolio Pipeline)
val BranchKnowledge = Color(0xFF818CF8)    // Indigo (Knowledge Loop)
val BranchCerts = Color(0xFF94A3B8)        // Slate (Certifications)
val BranchLanguage = Color(0xFFE2E8F0)     // Cool Slate (Second Language)

// Typography & Content Colors (Slate-100 down to Slate-600)
val TextPrimary = Color(0xFFF1F5F9)        // Slate-100 high contrast
val TextSecondary = Color(0xFF94A3B8)      // Slate-400 balanced body
val TextMuted = Color(0xFF64748B)          // Slate-500 metadata
val TextDarkMuted = Color(0xFF475569)      // Slate-600 subtle
val TextAccent = Color(0xFF818CF8)         // Indigo-400 prominent accents
